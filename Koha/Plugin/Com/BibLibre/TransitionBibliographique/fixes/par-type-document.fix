# title: Par type de document
# description: Garder les notices qui ont un type de document particulier (par exemple: 319, 039, 995r ou 200b)

# S'il y a une donnée exemplaire en 995r qui est égal à 'Livre'

if marc_match(995r,'Livre')
  # Garde
else
  # Si je n'ai pas de 995r, mais que j'ai un 200$b égal à 'Livres'
  if marc_match(200b,'Livres')
    # Garde
  else
    reject()
  end
end
